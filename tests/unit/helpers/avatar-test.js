import { avatar } from 'banmanager-web-client/helpers/avatar';
import { module, test } from 'qunit';

module('Unit | Helper | avatar');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = avatar([42]);
  assert.ok(result);
});
